To clarify the testing structure of this application, all of the modules work with the user interface.
Unit tests are not possible for UI components, so all of the testing is Selenium integration tests
to check that the expected UI is present and functioning.